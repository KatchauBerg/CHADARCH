# workflow: snappy
# Snappy desktop — no animations or gaps, keeps visual effects
$WORKFLOW_ICON=󰓅
$WORKFLOW_DESCRIPTION = Snappy — no animations, tight layout

decoration {
    rounding = 0
}

general {
    gaps_in    = 0
    gaps_out   = 0
    border_size = 1
}

animations:enabled = false
