# apps.conf — Default application variables
# Managed by dotfiles — edit templates/hypr/apps.conf
# These variables are used by keybindings.conf

$terminal    = kitty
$fileManager = dolphin
$browser     = firefox
$editor      = code
$menu        = rofi -show drun -theme ~/.config/rofi/launcher.rasi
