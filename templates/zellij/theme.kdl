// Theme: Generated by theme-switch.sh
// Do not edit directly - modify templates instead

themes {
    current {
        fg "#{{TEXT}}"
        bg "#{{BASE}}"
        black "#{{CRUST}}"
        red "#{{RED}}"
        green "#{{ACCENT}}"
        yellow "#{{YELLOW}}"
        blue "#{{BLUE}}"
        magenta "#{{MAUVE}}"
        cyan "#{{TEAL}}"
        white "#{{TEXT}}"
        orange "#{{PEACH}}"
    }
}
